/**
 * Global Loading Component - 全局加载组件
 * 
 * Next.js 会自动在页面级别显示此组件
 * 当路由切换或页面加载时显示
 * 
 * 注意：返回 null 避免切换 conversation 时闪屏
 * 
 * @module app/loading
 */

/**
 * 全局加载状态组件
 * 
 * 返回 null 避免路由切换时的闪屏
 */
export default function GlobalLoading() {
  return null
}

